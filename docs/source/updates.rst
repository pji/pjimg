.. _updates::

Updates
~~~~~~~
The following are notes on the changes in each version of :mod:`pjimg`.


Updates in v0.1.0
=================
The following were the updates in :mod:`pjimg` v0.1.0:

*   Created this update page.
*   (Not started.) Replaced :class:`numpy.ndarray` objects with
    :mod:`pytorch.Tensor` objects.
*   (Not started.) Replaced `precommit.py` with `make` automation.
*   (Not started.) Replaced :mod:`pipenv` with :mod:`poetry`.
*   (Not started.) Set up multiversion testing with :mod:`tox`.
